services:
  trusthash-sidecar:
    environment:
      - NODE_ENV=development
      - LOG_LEVEL=debug
    restart: 'no'
    # Mount source for development hot reload
    volumes:
      - ./src:/app/src:ro
      - ./data:/app/data

  trusthash-proxy:
    restart: 'no'
