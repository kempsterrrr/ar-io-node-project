# Docker runtime configuration for Trusthash Sidecar
# This file is used by docker-compose.yaml via env_file.
# For signing config, add ENABLE_SIGNING etc. to .env (gitignored).

# Gateway Connection (container-to-container)
GATEWAY_URL=http://core:4000
