# =============================================================================
# Image Provenance Sidecar Configuration
# =============================================================================

# Server
PORT=3003
NODE_ENV=development
LOG_LEVEL=debug

# Docker Proxy (only used with docker-compose)
# External port for nginx proxy (default: 3003)
PROXY_PORT=3003

# DuckDB Database
# Path to the persistent DuckDB database file
DUCKDB_PATH=./data/provenance.duckdb

# Gateway Connection
# Internal gateway URL (use core:4000 when running in Docker)
GATEWAY_URL=http://localhost:3000

# Turbo Upload Service
# URL for Arweave uploads via Turbo
TURBO_GATEWAY_URL=https://turbo.ardrive.io

# Arweave Wallet
# Path to the Arweave wallet JSON file for signing uploads
ARWEAVE_WALLET_FILE=./wallets/arweave-wallet.json

# ArNS Configuration
# Root ArNS name that you own (undernames will be created under this)
# The ANT process ID is automatically looked up from the ArNS registry
# Network (testnet/mainnet) is determined by NODE_ENV (development=testnet, production=mainnet)
ARNS_ROOT_NAME=

# ArNS Auto-Purchase Settings
# Automatically purchase more undername capacity when running low (default: true)
ARNS_AUTO_PURCHASE_CAPACITY=true
# Number of undernames to purchase at a time (default: 100)
ARNS_CAPACITY_PURCHASE_QTY=100
# Trigger auto-purchase when available capacity falls below this threshold (default: 10)
ARNS_CAPACITY_THRESHOLD=10

# Image Processing
# Maximum image size in MB (default: 50)
MAX_IMAGE_SIZE_MB=50
# Thumbnail width in pixels (default: 400)
THUMBNAIL_WIDTH=400
# Thumbnail JPEG quality 1-100 (default: 80)
THUMBNAIL_QUALITY=80

# C2PA Configuration
# Path to X.509 certificate for signing manifests (PEM format)
C2PA_CERT_PATH=./certs/certificate.pem
# Path to private key for signing manifests (PEM format)
C2PA_KEY_PATH=./certs/private-key.pem
# Optional: Timestamp Authority URL for trusted timestamps
# C2PA_TSA_URL=http://timestamp.digicert.com
