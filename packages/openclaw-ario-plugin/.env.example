# OpenClaw AR.IO Gateway Plugin Configuration
# Copy to .env and fill in values

# Required: Anthropic API key for Claude
ANTHROPIC_API_KEY=sk-ant-...

# OpenClaw gateway token for UI authentication
# Set a fixed token to avoid browser cache issues after restarts
# Access UI at: http://localhost:18789/?token=<this-value>
# Generate one with: openssl rand -hex 16
OPENCLAW_GATEWAY_TOKEN=

# Optional: Port overrides
OPENCLAW_PORT=18789
OPENCLAW_BRIDGE_PORT=18790
