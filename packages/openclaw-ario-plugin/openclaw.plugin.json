{
  "id": "ario-gateway",
  "name": "AR.IO Gateway Plugin",
  "version": "0.1.0",
  "description": "Tools for interacting with AR.IO gateways and Arweave, including SSH-based gateway management",
  "configSchema": {
    "type": "object",
    "properties": {
      "gatewayUrl": {
        "type": "string",
        "description": "URL of the AR.IO gateway API (e.g., http://10.0.0.2:4000 or http://core:4000)"
      },
      "timeout": {
        "type": "number",
        "description": "Request timeout in milliseconds"
      },
      "ssh": {
        "type": "object",
        "description": "SSH configuration for gateway management operations",
        "properties": {
          "host": {
            "type": "string",
            "description": "Gateway server hostname or IP (e.g., 10.0.0.2)"
          },
          "user": {
            "type": "string",
            "description": "SSH username",
            "default": "root"
          },
          "keyPath": {
            "type": "string",
            "description": "Path to SSH private key file (e.g., /home/node/.ssh/gateway_key)"
          }
        },
        "required": ["host", "keyPath"]
      }
    },
    "required": ["gatewayUrl"]
  }
}
