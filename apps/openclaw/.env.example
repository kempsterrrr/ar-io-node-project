# =============================================================================
# OpenClaw Agent Configuration
# =============================================================================
# Copy this file to .env and fill in the values.
# Generate tokens with: openssl rand -hex 32
# =============================================================================

# Required: Claude API key
ANTHROPIC_API_KEY=sk-ant-...

# Required: Token for OpenClaw UI authentication
# Generate: openssl rand -hex 32
OPENCLAW_GATEWAY_TOKEN=

# Optional: Keyring password for encrypted storage
# Generate: openssl rand -hex 32
OPENCLAW_KEYRING_PASSWORD=

# =============================================================================
# SSH Key Setup
# =============================================================================
# Place the gateway SSH private key in this directory as 'gateway_key'
# The key must have permissions 600 (chmod 600 gateway_key)
#
# To generate and set up the key:
#   ssh-keygen -t ed25519 -f gateway_key -N ""
#   ssh-copy-id -i gateway_key.pub root@10.0.0.2
#
# Or copy from the server:
#   scp root@OPENCLAW_SERVER:/root/.ssh/gateway_key ./gateway_key
# =============================================================================
